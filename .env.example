# Base de datos
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mochi_matcha
DB_USER=root
DB_PASSWORD=tu_password

# JWT
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga
JWT_EXPIRES_IN=8h

# Servidor
PORT=3001
NODE_ENV=development

# CORS — origen del frontend
CORS_ORIGIN=http://localhost:5173
